#include "main.h"
/**
 *_isupper:- checks for upppercase character
 *@c: parameter
 *Return: return 1 if c is uppercase and 0 otherwise
 *
 * */

int _isupper(int c)
{
if (c >= 'a' && c <= 'z')
{
return (0);

}
else
{
return (1);
}
}
